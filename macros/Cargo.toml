[package]
name = "bevywind-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
bevywind-core = { path = "../core" }
proc-macro2 = { version = "1", features = ["span-locations"] }
quote = "1"
syn = { version = "2", features = ["parsing"] }
